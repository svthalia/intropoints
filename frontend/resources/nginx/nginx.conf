server {
    # Internal frontend server
    listen       80;
    server_name  _;

    #   Redirect for main display
    location / {
        root   /usr/share/nginx/html/;
        index  index.html;
        try_files $uri $uri/ /index.html;
    }
}
